{{! draft code}}
<h1>Here's what's going on:</h1>
<div>
    {{#each posts as |post|}}

    {{> post-info post }}
</div>

{{else}}
<h5 class="pb-4 text-centered">There are currently no events</h5>
{{/each}}