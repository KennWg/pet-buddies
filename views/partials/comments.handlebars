{{! draft code - This doesn't get changed}}

<div class="comments">
  {{#each this}}
  <section class="comment">
    <div class="meta">
      <a href="/user/{{user.id}}">{{user.username}} on {{format_date created_at}}</a>
    </div>
    <div class="text">
      {{comment}}
    </div>
  </section>
  {{/each}}
</div>