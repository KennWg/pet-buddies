{{! draft code - This doesn't get changed}}

<div class="comments">
  {{#each this}}
  <section class="comment">
    <div class="meta">
      {{user.username}} on {{format_date created_at}}
    </div>
    <div class="text">
      {{comment}}
    </div>
  </section>
  {{/each}}
</div>